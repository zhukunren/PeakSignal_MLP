# Saved Models

Put model files that should be tracked by Git in this directory.

Allowed by `.gitignore`:

- `*.pkl`
- `*.joblib`
- `*.json`

Model files outside this directory are still ignored by default.
